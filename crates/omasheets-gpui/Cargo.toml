[package]
name = "omasheets-gpui"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# Same vendored kit Ashlar and Loom pin (see ashlar ui/loom-source.json).
gpui-kit = { path = "../../../loom/vendor/gpui-kit/crates/kit" }
omasheets-calc = { path = "../omasheets-calc" }
omasheets-core = { path = "../omasheets-core" }
omasheets-store = { path = "../omasheets-store" }
omasheets-xlsx = { path = "../omasheets-xlsx" }
zip = { version = "=8.6.0", default-features = false, features = ["deflate"] }

[dev-dependencies]
gpui-kit = { path = "../../../loom/vendor/gpui-kit/crates/kit", features = ["test-support"] }
omasheets-core = { path = "../omasheets-core" }
